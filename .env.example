# Copy to .env (gitignored). `just` loads it into every recipe's environment
# (set dotenv-load) — deliberate for dev; keep secrets out of tracked files.

# Pexels stock-photo source (https://www.pexels.com/api/ — free key).
# Optional: without it the Stock photos button returns a setup hint.
IRIS_PEXELS_API_KEY=
